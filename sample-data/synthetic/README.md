# Synthetic development photos

These five images were generated with Codex built-in Image Gen and checked against the pre-recorded packing-list expectations. The underlying image model identifier was not exposed by the tool.

## Accepted set

Initial delivery (`initial/`): six objects, wrong towel SKU, one extra soap unit, and an obscured candle SKU across three repeated views.

Corrected delivery (`corrected/`): five objects, replacement towel `UNIT-07`, no extra soap, and the candle SKU uncovered across two repeated views.

## Generation prompt specification

Create a photorealistic landscape phone photograph on the same light wooden table in neutral daylight. Show the exact controlled household objects and attach large, sharply readable white labels containing the specified SKU and persistent `UNIT-XX`. Preserve each physical object's identity and UNIT ID across views; never clone or add an object. The initial set contains `MUG-BLUE-12 / UNIT-01`, `TOWEL-SAND-20 / UNIT-02`, three `SOAP-LAV-100` units (`UNIT-03`, `UNIT-04`, `UNIT-05`), and `CANDLE-VAN-08 / UNIT-06` with only the candle SKU covered. The corrected set removes `UNIT-02` and `UNIT-05`, adds `TOWEL-SAND-02 / UNIT-07`, and reveals `CANDLE-VAN-08`. Do not add hands, people, logos, barcodes, UI, captions, paper sheets, or any other product.

## Disclosure

These are deterministic development fixtures, not photographs of actual physical contents. Do not present them as real delivery evidence. Recreate the same arrangement with real objects and phone photos for the final hiring submission.
